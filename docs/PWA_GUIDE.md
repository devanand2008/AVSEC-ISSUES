# PWA Guide

Canonical guide: `../PWA_GUIDE.md`.

Manifest, icons, service worker and offline page live under `apps/web/public`.
Authenticated secrets and private API responses must not be cached insecurely.
