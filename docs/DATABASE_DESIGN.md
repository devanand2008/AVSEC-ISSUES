# Database Design

Canonical design: `../DATABASE_DESIGN.md`.

The production data model is Prisma/PostgreSQL with UUID primary keys,
relations, indexes, audit logs, session tables, role/scope records, attendance,
issue, announcement, notification, storage and QR/feedback structures.
