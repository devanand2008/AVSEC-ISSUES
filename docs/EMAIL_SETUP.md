# Email Setup

Related guides: `../ANNOUNCEMENT_AND_EMAIL_GUIDE.md`, `../FIREBASE_SETUP.md`.

Configure email through `.env` using `EMAIL_*` and `SMTP_*` variables. Email
failures must not cancel in-app announcement publication.
